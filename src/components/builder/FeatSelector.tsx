import React from 'react';

interface FeatSelectorProps {
  // TODO: Props definieren
}

export default function FeatSelector(props: FeatSelectorProps) {
  return (
    <div>
      <h2>FeatSelector</h2>
      {{/* Inhalte folgen */}}
    </div>
  );
}
