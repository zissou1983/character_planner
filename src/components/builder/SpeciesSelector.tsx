import React from 'react';

interface SpeciesSelectorProps {
  // TODO: Props definieren
}

export default function SpeciesSelector(props: SpeciesSelectorProps) {
  return (
    <div>
      <h2>SpeciesSelector</h2>
      {{/* Inhalte folgen */}}
    </div>
  );
}
