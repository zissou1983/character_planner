// src/environments/environment.ts

export const environment = {
    production: false,  // Hier wird `false` gesetzt, da es sich um die Entwicklungsumgebung handelt
    apiUrl: 'http://localhost:3000/api' // Beispiel-API-Endpunkt, falls du eine API lokal im Entwicklungsmodus nutzt
  };
  