import React from 'react';

interface BackgroundSelectorProps {
  // TODO: Props definieren
}

export default function BackgroundSelector(props: BackgroundSelectorProps) {
  return (
    <div>
      <h2>BackgroundSelector</h2>
      {{/* Inhalte folgen */}}
    </div>
  );
}
